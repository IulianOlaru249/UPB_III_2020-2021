variable w ::= { a, b, c };
%grammarFromTHESKYYYYY ::= Grammar (
variable x ::= { a, b, c };
GRgrammar ::= Grammar (
	nonterminals :: { S, S1, S2 } ;
	% alphabet :: { ana, are } ;
	alphabet :: { a, b } ;
	% alphabet :: { multe, mere } ;
	start_symbol :: S ;

	S -> a&S | b&S | a&S1 ;
	S1 -> b&S2 | e ;
	S2 -> a&S2 | aa ;
) ;;

% ana are mere
% shame

/*
	This is a comment on multiple lines
*/

%automata1 
automata1 ::=  PushDownAutomata (
	alphabet :: { a, b, c, d } ;
	stack_alphabet :: { Z0, X } ;
	initial_state :: q0 ;
	states :: { q0, q1, q2, q3, q4 } ; 
	final_states :: { q4 } ;
	variable y ::= { a, b } ;

	q0 -> (
		y, z0 / X&Z0 -> q1 ;
	) ;

	q1 -> (
		a, X / X&X -> q1 ;
		b, X / e -> q2 ;
		a, X / X&X -> q1 ;
	) ;
	
	q2 -> (
		b, X / e -> q2 ;
		c, Z0 / X&Z0 -> q3 ;
	) ;
	
	q3 -> (
		c, X / X&X -> q3 ;
		d, X / e -> q4 ;
	) ;

	q4 -> (
		d, X / e -> q4 ;
		e, Z0 / Z0 -> q4 ;
	) ;
) ;;

/*
	This is a comment on multiple lines
*/
